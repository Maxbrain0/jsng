{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}

<div class="j-cover-image">
  {{#if @blog.logo}}
  <img src="{{@blog.cover_image}}" alt="Cover Image">
  <div class="j-image-text">
    <h1>{{@blog.title}}</h1>
    <h2>{{@blog.description}}</h2>
  </div>
  {{/if}}
</div>

<main role=""main>
  {{>posts-list}}
</main>

{{pagination}}